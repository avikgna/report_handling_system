{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install",
          "buildCommand": "npm run build"
        }
      }
    ],
    "build": {
      "env": {
        "PRISMA_GENERATE_IN_POSTINSTALL": "true"
      }
    }
  }
